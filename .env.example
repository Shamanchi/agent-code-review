# Порог длины функции в строках для правила long-function.
MAX_FUNCTION_LINES=50
# Границы весов правил при самообучении.
MIN_WEIGHT=0.1
MAX_WEIGHT=2.0
# Шаги изменения веса на accept / reject фидбек.
UPVOTE_STEP=0.1
DOWNVOTE_STEP=0.2
# Хост и порт API.
APP_HOST=0.0.0.0
APP_PORT=8000
